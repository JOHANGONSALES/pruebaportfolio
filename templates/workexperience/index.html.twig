{% extends 'base.html.twig' %}

{% block title %}Workexperience index{% endblock %}

{% block body %}
    <h1>Workexperience index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>Id</th>
                <th>Empresa</th>
                <th>Cargo</th>
                <th>Descripcion</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for workexperience in workexperiences %}
            <tr>
                <td>{{ workexperience.id }}</td>
                <td>{{ workexperience.empresa }}</td>
                <td>{{ workexperience.cargo }}</td>
                <td>{{ workexperience.descripcion }}</td>
                <td>
                    <a href="{{ path('app_workexperience_show', {'id': workexperience.id}) }}">show</a>
                    <a href="{{ path('app_workexperience_edit', {'id': workexperience.id}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="5">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_workexperience_new') }}">Create new</a>
{% endblock %}
