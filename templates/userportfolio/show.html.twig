{% extends 'base.html.twig' %}

{% block title %}Userportfolio{% endblock %}

{% block body %}
    <h1>Userportfolio</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ userportfolio.id }}</td>
            </tr>
            <tr>
                <th>Nombre</th>
                <td>{{ userportfolio.nombre }}</td>
            </tr>
            <tr>
                <th>Email</th>
                <td>{{ userportfolio.email }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ userportfolio.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_userportfolio_index') }}">back to list</a>

    <a href="{{ path('app_userportfolio_edit', {'id': userportfolio.id}) }}">edit</a>

    {{ include('userportfolio/_delete_form.html.twig') }}
{% endblock %}
